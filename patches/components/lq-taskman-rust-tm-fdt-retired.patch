diff --git a/taskman/Makefile b/taskman/Makefile
--- a/taskman/Makefile
+++ b/taskman/Makefile
@@ -78,5 +78,8 @@ ifneq ($(QSOE_RUST_TM_ELF),1)
 $(error QSOE_RUST_TM_ELF must be 1 after C tm_elf retirement)
 endif
+ifneq ($(QSOE_RUST_TM_FDT),1)
+$(error QSOE_RUST_TM_FDT must be 1 after C tm_fdt retirement)
+endif
 ifneq ($(QSOE_RUST_TM_PATHMGR),1)
 $(error QSOE_RUST_TM_PATHMGR must be 1 after C tm_pathmgr retirement)
 endif
@@ -153,8 +156,5 @@ TASKMAN_PSEUDODEV_OBJS :=
 TASKMAN_RSRCDB_OBJS :=
 TASKMAN_FDT_OBJS :=
-ifeq ($(QSOE_RUST_TM_FDT),0)
-TASKMAN_FDT_OBJS += $(OBJDIR)/sys/fdt.o
-endif
 
 TASKMAN_SYSMAP_OBJS :=
 TASKMAN_OBJS := \
