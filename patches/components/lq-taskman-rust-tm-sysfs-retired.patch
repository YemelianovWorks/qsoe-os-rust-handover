diff --git a/taskman/Makefile b/taskman/Makefile
--- a/taskman/Makefile
+++ b/taskman/Makefile
@@ -69,5 +69,8 @@ ifneq ($(QSOE_RUST_TM_SYSMAP),1)
 $(error QSOE_RUST_TM_SYSMAP must be 1 after C tm_sysmap retirement)
 endif
+ifneq ($(QSOE_RUST_TM_SYSFS),1)
+$(error QSOE_RUST_TM_SYSFS must be 1 after C tm_sysfs retirement)
+endif
 
 TASKMAN_DIR := $(TOP)/taskman
